# ===== 绘梦工坊 · 本地环境变量模板 =====
# 用法：复制本文件为 .env（同目录），填入自己的密钥；.env 已被 .gitignore 排除，永不上传
#
# 一、AI 视频生成引擎（第 7 课）
#   provider=mock   ：默认，无 Key 也能跑通全流程（视频为占位演示）
#   provider=seedance：火山方舟 Seedance 真实生成，必须配下面三项
AI_VIDEO_PROVIDER=mock
ARK_API_KEY=在此填入你自己的火山方舟 API Key
ARK_MODEL=doubao-seedance-1-5-pro-251215
# ARK_BASE_URL=https://ark.cn-beijing.volces.com/api/v3   # 默认即可，无需改

# 二、安全加固（建议）
# JWT_SECRET=生产环境请换成 >=32 字节的随机串（生成：openssl rand -hex 32）
